# CLAUDE.md —— 类案研究项目实践画像（模板）

> **这是一份模板。** 复制为 `CLAUDE.md` 后，把下面【】里的内容改成你的真实偏好即可。
> `CLAUDE.md` 已被 `.gitignore`（含个人署名等隐私），不会进入版本库；改这一个文件就能调整所有报告风格，无需动 Skill。
>
> ```bash
> cp CLAUDE.example.md CLAUDE.md   # 然后编辑 CLAUDE.md
> ```
>
> 说明：本文件以 “Claude Code” 为例命名（项目历史原因），但**任何能读取项目内 Markdown 指令的 AI 编程工具**都可据此套用偏好（见 `AGENTS.md`）。

## 项目说明
这是一个基于案例库（默认北大法宝 MCP，亦支持自带数据，见 `AGENTS.md`）的类案检索与分析项目。
- 标准类案检索 → `skills/case-research/SKILL.md`
- 集团/群体性诉讼（核心判决筛选）→ `skills/group-litigation-research/SKILL.md`

当我请求“做一次类案研究/类案检索/类案分析”时，请加载对应 Skill 并严格按其三步两检查点流程执行。

## 使用者信息
- 角色：【填你的角色，如 法律研究者 / 诉讼律师 / 法务】
- 称谓：【填你希望 AI 怎么称呼你，如 老师 / 律师】

## 报告文风偏好
- 语言风格：【如 严谨学术兼顾可读性】
- 默认场景：【居中 / 攻方 / 辩方，常用"居中"】
- 篇幅倾向：【详尽 / 精炼】

## 报告固定格式要素
- 报告主题：【填你常做的主题，或每次研究时再定】
- 抬头/封面：标题 + 报告人 + 日期；【是否需要学号/单位】
- 章节编号风格：一、（一）1.（1）
- 署名：【填报告署名】

## Excel 偏好
- 字体：【默认微软雅黑 / 等线，按需填】
- 是否需要导出为特定模板：【是 / 否】

## 通用纪律（默认开启，一般不用改）
- 严格遵守 SKILL.md 的反幻觉铁律：不编案号、不编案件、引用必带案号+法院+链接；交付前过 `scripts/verify_report.py` 自检。
- 两个检查点必须停下等我确认，不得擅自跳过。
- 数据结论必须有 06_analytics.json 的数字支撑。
- 区分普通案例（进分析）与经典/评析案例（进附录）。
